# playback-worker secrets / protected config
# Use Cloudflare-managed secrets where values are sensitive.

# Optional shared secret for internal calls during network-trust migration.
PULSEFM_INTERNAL_CALLER_TOKEN=

# If required for auxiliary migration scripts.
PULSEFM_SOURCE_GCP_PROJECT_ID=
PULSEFM_SOURCE_GCP_SERVICE_ACCOUNT_KEY=
